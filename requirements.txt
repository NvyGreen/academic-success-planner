# Runtime dependencies — what the deployed app imports and runs.
# Direct deps only, pinned to versions verified working on Python 3.13.
Flask==3.1.1
python-dotenv==1.1.1
Flask-WTF==1.2.2
email-validator==2.2.0
passlib==1.7.4
schedule==1.2.2
